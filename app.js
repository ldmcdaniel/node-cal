#!/usr/bin/env node

var zellers = require('./lib/zeller');

console.log(zellers);

// process.argv.forEach(function(val, index, array) {
//   if(index >= 2 ) {
//     console.log(index + ': ' + val);
//   }
// });
